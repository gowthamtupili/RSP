#include "config.hpp"

namespace RadarConfig {
    Config load_config() {
        // TODO: Implement loading from a configuration file or command-line arguments
        // For now, return default configuration
        Config cfg;
        return cfg;
    }
}